{
  "name": "app",
  "private": "true",
  "dependencies": {
    "esbuild": "^0.14.54",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "remount": "^0.11.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets"
  }
}
